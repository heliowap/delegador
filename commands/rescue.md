---
description: Delega uma correcao localizada ao executor — gate de delegabilidade, laco com permissao em codigo e verificacao
argument-hint: "<o defeito em uma frase>"
---

Roteie este pedido para o subagente `delegador-rescue`.
A resposta final ao usuario e a saida do binario, sem parafrase.

Pedido bruto:

$ARGUMENTS

Antes de rotear, garanta que a sessao ja tem a evidencia verbatim: o trecho
com `arquivo:linha`, a mensagem de erro exata e a fonte do contrato. Sem
isso o `plan` reprova — e a reprova e informacao, nao um obstaculo a
contornar.
